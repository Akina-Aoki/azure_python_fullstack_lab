[project]
name = "azure-python-fullstack-lab"
version = "0.1.0"
description = "eClipseBord full-stack Solar and Lunar eclipse dashboard"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]
